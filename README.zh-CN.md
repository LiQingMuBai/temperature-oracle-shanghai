# 上海最高温量化预测系统

[English](README.md) | **简体中文**

面向上海最高气温预测市场的量化决策系统。它结合历史数值预报校准、实时多模式天气预报、Polymarket可成交订单簿、分层风险过滤、Telegram通知和不可回看的交易级滚动回测。

> 仅用于研究。系统不会自动执行真实下单，模型优势也不等于保证盈利。

## 策略全景

```mermaid
flowchart LR
    A["ZSPD历史实况"] --> D["偏差与概率校准"]
    B["ECMWF · GFS · CMA · JMA"] --> D
    C["天气特征<br/>露点 · 云量 · 辐射 · 降水 · 风"] --> D
    D --> E["整数温度概率分布"]
    F["Polymarket订单簿<br/>买价 · 卖价 · 深度"] --> G["可成交组合成本"]
    E --> H["信号引擎"]
    G --> H
    H --> I{"通过全部风控？"}
    I -- 是 --> J["Telegram下注计划<br/>并记录模拟交易"]
    I -- 否 --> K["无机会状态通知"]
    J --> L["结算与滚动回测"]
```

预测目标及结算参考站为上海浦东国际机场ZSPD。模型输出整数温度档概率，而不是只给出一个点预测。

## 信号决策漏斗

相邻双档或三档组合必须同时通过全部条件：

```mermaid
flowchart TD
    A["相邻温度组合"] --> B{"包含模型众数档？"}
    B -- 否 --> X["拒绝"]
    B -- 是 --> C{"至少3个原始模式支持？"}
    C -- 否 --> X
    C -- 是 --> D{"每档可成交价≥1¢？"}
    D -- 否 --> X
    D -- 是 --> E{"组合概率达标？"}
    E -- 否 --> X
    E -- 是 --> F{"概率－可成交成本<br/>达到优势阈值？"}
    F -- 否 --> X
    F -- 是 --> G{"资金与风险额度充足？"}
    G -- 否 --> X
    G -- 是 --> H["发送Telegram信号"]
```

历史偏差校准相对当日原始多模式中心最多移动±1°C。组合成本按订单簿逐层吃单计算，并要求所有温度档买入相同份数。

系统保留两套概率：天气独立概率负责计算交易优势；市场融合预测概率负责整数档排序和覆盖门槛。市场核心模式下，市场权重按时间层限制在35%/50%/60%/70%以内，并根据买卖价差和深度自动降低；约2小时盘口趋势只修正市场信号，不会反向抬高天气独立优势。组合必须包含盘口众数档；天气众数与盘口众数相差超过2°C时执行安全否决。

## 距离结算时间分层

生产任务同时监控今日和次日市场，剩余时间使用Polymarket官方事件结束时间计算。

| 分层 | 最低组合概率 | 最低模型优势 |
|---|---:|---:|
| 24h | 55% | 5% |
| 12h | 48% | 2% |
| 6h | 50% | 1% |
| 3h | 52% | 0.5% |

越临近结算，系统要求更高的绝对胜率，同时允许更小的定价优势。各时间层独立统计表现。

每个时间层都可以产生一笔独立模拟交易样本。每笔交易只包含1个整数温度档，或连续相邻的2档。

系统另设探索型价值通道：不包含盘口众数档的低价相邻组合，只要融合概率不低于20%、独立天气模型优势不低于10%，也可进入模拟交易；负优势交易始终不会入场。

融合概率以市场为主：24h/12h/6h/3h层的基础市场权重分别为60%/70%/80%/85%，价差过宽或挂单深度不足时自动下调。纯天气概率保持独立，继续用于计算价值优势，避免循环验证。

今日市场进入6h或3h层后，系统使用最新ZSPD METAR实况对概率分布进行条件化：低于当天已观测最高温的档位直接视为不可能。实况超过2小时未更新时只记录，不参与交易判断。

## 资金与成交策略

```mermaid
flowchart LR
    A["初始权益<br/>$10"] --> B["加入已结算盈亏"]
    B --> C["扣除未结算仓位<br/>占用现金"]
    C --> D["可用余额"]
    D --> E["单个日期合约<br/>最多20%权益"]
    E --> F["目标每档10份"]
    F --> G{"预算充足？"}
    G -- 是 --> H["Telegram发送成功后<br/>才记录模拟交易"]
    G -- 否 --> I["缩减份数<br/>最低5份"]
```

每次正式Telegram下注通知成功送达，就记录一笔模拟交易；无机会通知不计交易。当前信号与盈亏报告均未扣手续费，因此实际可实现收益可能低于回测值。

为增加回测样本而不继续放宽正式通知，系统并行运行两套影子组合：市场核心宽松策略和市场价格基准策略。每套策略在每个合约、每个时间层最多记录一笔固定1美元模拟交易；结果独立统计，不会作为下注建议发送。

## 天气模型回测

所有结果均采用时间顺序滚动回测，没有随机打乱数据。

### 增强D+1留档模型

区间：2025-01-19至2026-08-12，共571期。

| 模型 | MAE | RMSE | 整数档命中 | 相邻档命中 |
|---|---:|---:|---:|---:|
| 增强天气特征 | 0.862°C | 1.150°C | 38.0% | 82.8% |
| 在线元策略 | **0.856°C** | **1.143°C** | 38.7% | 82.5% |
| 离散温度档策略 | **0.837°C** | 1.194°C | **38.9%** | 82.5% |
| 原温度动态集合 | 0.995°C | 1.281°C | 32.0% | 78.5% |

实际最高温不低于30°C的160个高温日中，增强模型MAE为0.843°C，整数档命中率为41.9%。

### 多模式留档集合

D+1区间：2024-04-05至2026-08-12，共860期。

| 预报 | MAE | RMSE | 偏差 | 整数档命中 |
|---|---:|---:|---:|---:|
| 滚动校准集合 | **0.977°C** | **1.277°C** | -0.038°C | **33.1%** |
| 原始等权集合 | 1.800°C | 2.163°C | -1.669°C | 14.4% |
| GFS | 1.431°C | 1.806°C | -1.090°C | 21.0% |
| ECMWF | 1.524°C | 1.876°C | -1.278°C | 16.7% |

这些数字衡量天气预测能力，不代表交易收益率。

## 实时交易回测状态

实时数据库保存不可回看的模型状态、盘口、完整卖盘深度、候选组合、过滤结果、Telegram送达记录和结算结果。

生产服务每15分钟任务结束后还会发送Telegram健康心跳，分别报告今日和次日市场检查是否成功。

| 指标 | 当前状态 |
|---|---:|
| 实时快照次数 | 6 |
| 已保存温度档记录 | 66 |
| 未结算合约 | 2 |
| 正式模拟下注通知 | 0 |
| 已结算模拟交易 | 0 |

目前交易样本不足，不能估计胜率、收益率或最大回撤。在积累足够的已结算信号前，不应宣称交易策略有效。后续报告会分别展示24h/12h/6h/3h各层的命中率、盈亏和平均入场优势。

## 回测完整性

```mermaid
sequenceDiagram
    participant W as 天气模型
    participant P as Polymarket
    participant S as 快照数据库
    participant T as Telegram
    participant R as 结算结果
    W->>S: 保存当时可获得的预报
    P->>S: 保存当时可成交的盘口深度
    S->>T: 全部条件通过后发送正式信号
    T->>S: 发送成功才记录模拟交易
    R->>S: 写入最终获胜整数档
    S->>S: 计算盈亏、权益、回撤与分层统计
```

历史盘口不会被最终价格覆盖，从而避免交易级滚动回测中的未来数据泄漏。
